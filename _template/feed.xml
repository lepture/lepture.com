<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ context.sitename }}</title>
    <link href="{{ content_url(context.siteurl, folder, 'feed') }}" rel="self" />
    <link href="{{ content_url(context.siteurl, folder) }}" />
    <updated>{{now|xmldatetime}}</updated>
    <id>{{ context.siteurl }}</id>
    {% for rst in rsts -%}
    <entry>
        <title type="html"><![CDATA[{{ rst.parts.title }}]]></title>
        <author><name>{{ context.author }}</name><uri>{{ context.siteurl }}</uri></author>
        <link href="{{ content_url(context.siteurl, rst.slug) }}"/>
        <updated>{{ rst.get_info('date')|xmldatetime }}</updated>
        <published>{{ rst.get_info('date')|xmldatetime }}</published>
        <id>{{ content_url(context.siteurl, rst.slug) }}</id>
        {% for tag in rst.get_info('tags', []) -%}
        <category scheme="{{ content_url(context.siteurl, 'tag', tag) }}" term="{{tag}}" label="{{ tag|title }}" />
        {% endfor -%}
        <content type="html" xml:base="{{ context.siteurl }}" xml:lang="en">
            <![CDATA[{{ rst.parts.body }}]]>
        </content>
    </entry>
    {% endfor -%}
</feed>
